<?php

/** @var \Illuminate\Database\Eloquent\Factory $factory */

use App\Solicitud;
use Faker\Generator as Faker;

$factory->define(Solicitud::class, function (Faker $faker) {
    return [
        // 'numero_solicitud' => $faker->randomNumber(5),
        // 'numero_operador' => $faker->randomNumber(5),
        // 'nombre_operador' => $faker->name,
        // 'numero_asociado' => $faker->randomNumber(5),
        // 'nombre_asociado' => $faker->name,
        // 'numero_cliente' => $faker->randomNumber(5),
        // 'nombre_cliente' => $faker->name,
        // 'nombre_asociado' => $faker->randomNumber(5),
        // 'nombre_asociado' => $faker->randomNumber(5),
        // 'nombre_asociado' => $faker->randomNumber(5),
        // 'nombre_asociado' => $faker->randomNumber(5),
    ];
});
